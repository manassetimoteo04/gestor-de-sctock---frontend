// REFACTORING THE CODE

class ClientApp {
  constructor() {
    // SELECIONANDO AS VARIÁVEIS
    // this.date = document.querySelector(".");
  }

  // FUNCÇÕES DOS EVENT LISTENERS
}
const client = new ClientApp();
export { client };
